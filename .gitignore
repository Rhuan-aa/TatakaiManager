# Ambiente de produção/local — nunca versionar (contém segredos)
.env
*.env
!.env.example

# Docs de trabalho — mantidas apenas localmente, fora do repositório remoto
DEPLOY.md
HANDOFF.md

# OS / editor
.DS_Store
Thumbs.db
