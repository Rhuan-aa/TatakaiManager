# TLS (Let's Encrypt) e WSS automáticos. Troque os domínios pelos seus (DuckDNS).
# O Caddy faz proxy transparente do upgrade WebSocket — nada extra é necessário.

tatakai.duckdns.org {
    reverse_proxy frontend:80
}

fightapi.duckdns.org {
    reverse_proxy backend:8080
}
