// Placeholder de desenvolvimento. Em produção, o container gera este arquivo
// a partir das variáveis de ambiente (API_URL / WS_URL). Em dev fica vazio e
// a aplicação cai no fallback de import.meta.env / localhost (ver src/config.js).
window.__ENV__ = {};
